1. hola mundo

import java.util.*;

public class Main {
    public static void main(String[] args) {
      System.out.println("Hello World");
  }
}

2. mínimo de 2 números
import java.util.Scanner;

public class Main
{
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
        int num1 = sc.nextInt();
        int num2 = sc.nextInt();
		
		if (num1 > num2) {
		    System.out.println("El segundo número es menor");
		} else if (num1 < num2) {
    		System.out.println("El primer número es menor");
		} else {
            System.out.println("Los números son iguales");
        }
	}
}

3. Factorial de un numero n (con recursividad)
import java.util.Scanner;

public class Main
{
    public static int factorial(int numero) {
        if (numero <= 1) {
            return 1;
        } else {
            return numero * factorial(numero - 1);
        }
    }
	public static void main(String[] args) {
	    Scanner sc = new Scanner(System.in);
        int num = sc.nextInt();

        System.out.println("El factorial de " + num + " es: " + factorial(num));
	}
}
4. Serie Fibonacci (100 numeros)
import java.util.ArrayList;

public class Main
{

    public static ArrayList listaFibonaciFuncionRecursiva(ArrayList<Integer> lista, int ultimaPosicion) {
        if (ultimaPosicion < 100) {
            lista.add(lista.get(ultimaPosicion) + lista.get(ultimaPosicion - 1));
            return listaFibonaciFuncionRecursiva(lista, ultimaPosicion + 1);
        } else {
            return lista;
        }
    }
	public static void main(String[] args) {
        ArrayList<Integer> listaFibonaci = new ArrayList<Integer>();
        listaFibonaci.add(0);
        listaFibonaci.add(1);
        listaFibonaci = listaFibonaciFuncionRecursiva(listaFibonaci, 1);
        System.out.println(listaFibonaci); 
	}
}
5. Generar 11 números aleatorios y hacer media, mediana y moda
import java.util.ArrayList;

public class Main
{
    public static int media(ArrayList<Integer> lista) {
        int suma = 0;
        for (int numero: lista) {
            suma += numero;
        }
        return suma;
    }
    public static int mediana(ArrayList<Integer> lista) {
        lista = lista.sort();
        System.out.println("Lista ordenada de menor a mayor" + lista);
        return lista.get(5);
    }
    public static int moda(ArrayList<Integer> lista) {
        HashMap<Integer, Integer> aparicionesNumero = new HashMap<>();
        
        for (int numero : lista) {
            aparicionesNumero.put(numero);
        }
    }
	public static void main(String[] args) {
	    ArrayList<Integer> listaNumeros = new ArrayList<Integer>();
	    for (int i = 0; i < 11; i++) {
            listaNumeros.add((int)(Math.random() * 13)); 
	    }
        System.out.println(listaNumeros);
        
        System.out.println("Media: " media(listaNumeros));
        System.out.println("Mediana" + mediana(listaNumeros));
        System.out.println("Moda: " + moda(listaNumeros));
    } 
}

6. Criba de erastóstenes para entre 0 y un numero dado
import java.util.Scanner;
import java.util.ArrayList;

public class Main
{

    
    public static ArrayList<Integer> crearListaPrimos (int numero) {
        ArrayList<Integer> listaPrimos = new ArrayList<Integer>();
        for (int i = 2; i < numero; i++) {
            boolean primo = true;
            for (int j = 2; j * j <= i; j++) {
                if (i % j == 0) {
                    primo = false;
                    break;
                }
            }
            if (primo) {
                listaPrimos.add(i);
            }
        }
        return listaPrimos;
    }

	public static void main(String[] args) {
		
        Scanner scanner = new Scanner(System.in);

        System.out.print("Introduce el número hasta el que se revisarán los primos: ");
        int numero = scanner.nextInt(); 
        
        ArrayList<Integer> listaPrimos = crearListaPrimos(numero);
        System.out.print(listaPrimos);

	}
}
7. Debate las cuatro principales características de la POO
    1.
    2.
    3.
    4.
8. Clases vs Interfaces
Las interfaces no poseen código como tal, son solo un esqueleto que indica que funcionas debe tener de forma obligatoria una clase 
que extienda de dicha interfaz mientras que la clase posee el codigo que hace que funcionen los metodos

9. Haz un programa que demuestre la diferencia entre atributos y métodos adecuados
public class Persona {
    // Atributos (datos)
    private String nombre;
    private int edad;

    // Constructor
    public Persona(String nombre, int edad) {
        this.nombre = nombre;
        this.edad = edad;
    }

    // Método adecuado: muestra información de la persona
    public void mostrarInfo() {
        System.out.println("Nombre: " + nombre + ", Edad: " + edad);
    }

    // Método adecuado: aumenta la edad en 1
    public void cumplirAnos() {
        edad++;
    }

    // Getters y setters para encapsulamiento
    public String getNombre() {
        return nombre;
    }
    public void setNombre(String nombre) {
        this.nombre = nombre;
    }
    public int getEdad() {
        return edad;
    }
    public void setEdad(int edad) {
        if (edad >= 0) this.edad = edad;
    }
    
    public static void main(String[] args) {
        Persona p = new Persona("Ana", 30);
        p.mostrarInfo();
        p.cumplirAnos();
        p.mostrarInfo();
    }
}