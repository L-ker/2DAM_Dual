// Funcion simple de suma
const calcularNuevoMarcador = (puntajeActual, puntosASumar) => {
  return puntajeActual + puntosASumar;
};

// Exportamos la funcion para que el test la reciba
module.exports = { calcularNuevoMarcador };
